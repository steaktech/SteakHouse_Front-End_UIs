"use client";

import React from "react";
import { ExplorerWidget } from "@/app/components/Widgets/ExplorerWidget";

export default function ExplorePage() {
  return <ExplorerWidget isOpen={true} onClose={() => {}} />;
}