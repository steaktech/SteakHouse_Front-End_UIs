# Local development environment variables
# This file configures the app to use local Next.js API routes in the /api folder

# API Configuration - points to local Next.js API routes
NEXT_PUBLIC_API_BASE_URL=

# When NEXT_PUBLIC_API_BASE_URL is empty or not set, the API client will use 
# relative paths which automatically use the local Next.js /api routes

# Example: If you want to use an external API instead, uncomment and modify:
# NEXT_PUBLIC_API_BASE_URL=https://your-external-api.com/api

# Alternative backend URL (fallback option)
# NEXT_PUBLIC_BACKEND_URL=

# Note: With empty values above, the app will use local Next.js API routes:
# - /api/filteredTokens (for filtered token data)
# - /api/all-tokens (for all tokens)
# - /api/all-tokens-by-volume (for tokens sorted by volume)
# These routes serve the mock data defined in /app/api/mockData.ts
# 
# The mock data includes 10 tokens with different types:
# - 3 Meme tokens (ZEUS, ARES, DIONYSUS)
# - 2 Utility tokens (APOLLO, ATHENA) 
# - 2 AI tokens (HADES, POSEIDON)
# - 2 X-post tokens (HERA, ARTEMIS)
# All filtering and sorting functionality works out of the box!
